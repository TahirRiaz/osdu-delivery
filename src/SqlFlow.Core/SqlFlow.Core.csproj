<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>SqlFlow.Core</RootNamespace>
    <Description>OSDU Delivery core: the run model, run artifacts and events, secret references and hygiene, file stores, and flow identity. No database or engine dependencies.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    <PackageReference Include="System.Diagnostics.DiagnosticSource" />
  </ItemGroup>

  <ItemGroup>
    <!-- The test suite exercises engine-internal mechanisms (e.g. the watermark filtering reader) directly. -->
    <InternalsVisibleTo Include="SqlFlow.Core.Tests" />
  </ItemGroup>

</Project>

# Generated from old SQLFlow metadata (flw.PreIngestionCSV FlowID 547) by Generate-PreFlow.ps1.
# Stage 1 of 2 (pre): lands the CSV into [pre].[Baatbooking_trans] and builds the typed view v_Baatbooking_trans.
name: baatbooking_trans_01_csv
batch: BB
source:
  type: csv
  location: https://dwdatalakeprodv2.dfs.core.windows.net/datalakev2/raw/baatbooking/history/trans/
  options:
    srcFile: "trans*.csv"
    searchSubDirectories: "true"
    delimiter: ";"
    includeRowNumber: "false"
target:
  connection: ${env:SQLFLOW_CONN_DWPREPROD}
  schema: pre
  table: Baatbooking_trans
schema:
  evolve: widen
load:
  mode: append
incremental:
  dateColumn: FileDate_DW
  overlapDays: 0
transform:
  generateView: true
  columns:
    - { name: MEDIA_TYPE, expr: "CAST(@ColName AS int)" }
    - { name: MTM_SER_NR, expr: "CAST(@ColName AS int)" }
    - { name: USER_NR, expr: "CAST(@ColName AS int)" }
    - { name: SESSION_NR, expr: "CAST(@ColName AS DECIMAL(12,0))" }
    - { name: TRANS_NR, expr: "CAST(@ColName AS DECIMAL(11,0))" }
    - { name: TRANS_CODE, expr: "CAST(@ColName AS int)" }
    - { name: PRINT_CODE, expr: "CAST(@ColName AS int)" }
    - { name: AN_CODE, expr: "CAST(@ColName AS int)" }
    - { name: AN_PRINT, expr: "CAST(@ColName AS int)" }
    - { name: TRANS_DATE, expr: "CONVERT(datetime,@ColName,103)" }
    - { name: TICKET_NR, expr: "CAST(@ColName AS int)" }
    - { name: CLASS, expr: "CAST(@ColName AS int)" }
    - { name: NO_TARIFFS, expr: "CAST(@ColName AS int)" }
    - { name: NO_TRANSP, expr: "CAST(@ColName AS int)" }
    - { name: PAY_MODE, expr: "CAST(@ColName AS int)" }
    - { name: PRICE, expr: "CAST(@ColName AS int)" }
    - { name: VAT, expr: "CAST(@ColName AS int)" }
    - { name: PAY_CASH, expr: "CAST(@ColName AS int)" }
    - { name: PAY_OTHER, expr: "CAST(@ColName AS DECIMAL(9,0))" }
    - { name: CUST_ID, expr: "CAST(@ColName AS int)" }
    - { name: REFERENCE, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS DECIMAL(14,0)) ELSE NULL END" }
    - { name: NO_DETAIL, expr: "CAST(@ColName AS int)" }
    - { name: NO_CTRANS, expr: "CAST(@ColName AS int)" }
    - { name: NO_CARD, expr: "CAST(@ColName AS int)" }
    - { name: OP_MODE, expr: "CAST(@ColName AS int)" }
    - { name: RETURN, expr: "CAST(@ColName AS int)" }
    - { name: CPAY_MODE, expr: "CAST(@ColName AS int)" }
    - { name: EX_TICK_ID, expr: "CAST(@ColName AS int)" }
    - { name: EX_TICK_NR, expr: "CAST(@ColName AS int)" }
    - { name: EX_STATUS, expr: "CAST(@ColName AS int)" }
    - { name: CARD_TYPE, expr: "CAST(@ColName AS int)" }
    - { name: CARD_NR, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: SESS_ID, expr: "CAST(@ColName AS int)" }
    - { name: TRANS_STATUS, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: DUP_SEQ, expr: "CAST(@ColName AS int)" }
    - { name: TRANS_ID, expr: "CAST(@ColName AS int)" }
    - { name: INSERT_DATE, expr: "CONVERT(datetime,@ColName,103)" }
    - { name: COMP_ID, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: RSET_NR, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: ROUTE_NR, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: TOUR_NR, expr: "CAST(@ColName AS int)" }
    - { name: STOP_ID_FR, expr: "CAST(@ColName AS int)" }
    - { name: STOP_ID_TO, expr: "CAST(@ColName AS int)" }
    - { name: NO_TRITEM, expr: "CAST(@ColName AS int)" }
    - { name: RND_AMOUNT, expr: "CAST(@ColName AS int)" }
    - { name: RND_VAT, expr: "CAST(@ColName AS int)" }
    - { name: PASS_COUNT, expr: "CAST(@ColName AS int)" }
    - { name: TDAY, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: TMONTH, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: TYEAR, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: TWEEK, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: TDOW, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: TTIME, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: TDATE, expr: "CONVERT(datetime,@ColName,103)" }
    - { name: TNDOW, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: CUST_NAME, expr: "CAST(@ColName as varchar (50))" }
    - { name: CUST_ADDRESS, expr: "CAST(@ColName as varchar (50))" }
    - { name: CUST_PLACE, expr: "CAST(@ColName as varchar (50))" }
    - { name: CUST_REGION, expr: "CAST(@ColName as varchar (50))" }
    - { name: CUST_ZIP, expr: "CAST(@ColName as varchar (50))" }
    - { name: DEFAULT_REBATE, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: PAY_REF, expr: "CAST(@ColName AS DECIMAL(16,0))" }
    - { name: TRANSACTIONID, expr: "CAST(@ColName AS int)" }
    - { name: ACTOR_ID, expr: "CAST(@ColName AS int)" }
    - { name: PAY_CHANGE, expr: "CAST(@ColName AS int)" }
    - { name: MWEX_STATUS, expr: "CAST(@ColName AS int)" }
    - { name: FileRowDate_DW, expr: "CONVERT(datetime,@ColName,20)" }
    - { name: FileDate_DW, expr: "CAST(@ColName as decimal(14,0))" }
    - { name: FileSize_DW, expr: "CAST(@ColName as decimal(18,0))" }
    - { name: FileName_DW, expr: "CAST(@ColName as varchar (255))" }
    - { name: DataSet_DW, expr: "CAST(@ColName as numeric(14,0))" }

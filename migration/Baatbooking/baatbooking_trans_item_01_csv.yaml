# Generated from old SQLFlow metadata (flw.PreIngestionCSV FlowID 549) by Generate-PreFlow.ps1.
# Stage 1 of 2 (pre): lands the CSV into [pre].[Baatbooking_Trans_Item] and builds the typed view v_Baatbooking_Trans_Item.
name: baatbooking_trans_item_01_csv
batch: BB
source:
  type: csv
  location: https://dwdatalakestorev2prod.dfs.core.windows.net/datalakev2/raw/baatbooking/history/trans_item/
  options:
    srcFile: "trans_item*.csv"
    searchSubDirectories: "true"
    delimiter: ";"
    includeRowNumber: "false"
target:
  connection: ${env:SQLFLOW_CONN_DWPREPROD}
  schema: pre
  table: Baatbooking_Trans_Item
schema:
  evolve: widen
load:
  mode: append
incremental:
  dateColumn: FileDate_DW
  overlapDays: 0
transform:
  generateView: true
  columns:
    - { name: TRANS_ITEM_ID, expr: "CAST(@ColName AS int)" }
    - { name: MEDIA_TYPE, expr: "CAST(@ColName AS int)" }
    - { name: MTM_SER_NR, expr: "CAST(@ColName AS int)" }
    - { name: USER_NR, expr: "CAST(@ColName AS int)" }
    - { name: SESSION_NR, expr: "CAST(@ColName AS DECIMAL(12,0))" }
    - { name: TRANS_NR, expr: "CAST(@ColName AS DECIMAL(11,0))" }
    - { name: SEQ_NR, expr: "CAST(@ColName AS int)" }
    - { name: TICKET_NR, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: COMP_ID, expr: "CAST(@ColName AS int)" }
    - { name: FSET_NR, expr: "CAST(@ColName AS int)" }
    - { name: FGRP_NR, expr: "CAST(@ColName AS int)" }
    - { name: TARIFF_NR, expr: "CAST(@ColName AS int)" }
    - { name: CTARIFF, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: TAR_TYPE1, expr: "CAST(@ColName AS int)" }
    - { name: TAR_TYPE2, expr: "CAST(@ColName AS int)" }
    - { name: CLASS, expr: "CAST(@ColName AS int)" }
    - { name: REBATE_NR, expr: "CAST(@ColName AS int)" }
    - { name: REB_FACT, expr: "CAST(@ColName AS int)" }
    - { name: UNIT_COUNT, expr: "CAST(@ColName AS int)" }
    - { name: PASS_COUNT, expr: "CAST(@ColName AS int)" }
    - { name: SUM_PRICE, expr: "CAST(@ColName AS int)" }
    - { name: SUM_VAT, expr: "CAST(@ColName AS int)" }
    - { name: OP_MODE, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: NO_DETAIL, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: SESS_ID, expr: "CAST(@ColName AS int)" }
    - { name: TRANS_ID, expr: "CAST(@ColName AS int)" }
    - { name: REFERENCE, expr: "CAST(@ColName AS DECIMAL(14,0))" }
    - { name: ACTOR_ID, expr: "CAST(@ColName AS int)" }
    - { name: ITEM_TYPE, expr: "CAST(@ColName AS int)" }
    - { name: GROUP_ID, expr: "CASE WHEN LEN(@ColName ) > 0 THEN CAST(@ColName AS int) ELSE NULL END" }
    - { name: PRODUCT_ID, expr: "CAST(@ColName AS int)" }
    - { name: FARETYPE_NAME, expr: "CONVERT(varchar(50),@ColName)" }
    - { name: FileRowDate_DW, expr: "CONVERT(datetime,@ColName,20)" }
    - { name: FileDate_DW, expr: "CAST(@ColName as decimal(14,0))" }
    - { name: FileSize_DW, expr: "CAST(@ColName as decimal(18,0))" }
    - { name: FileName_DW, expr: "CAST(@ColName as varchar (255))" }
    - { name: DataSet_DW, expr: "CAST(@ColName as numeric(14,0))" }

# The migrations are generated by the EF Core tools; analyzer rules written for hand-authored code do not apply to the
# tool's output. This scopes the relaxation to this folder only; the rest of the module stays strict.
[*.cs]
generated_code = true
